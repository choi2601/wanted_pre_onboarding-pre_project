import React from 'react';
import styled from 'styled-components';

const MainPage = () => {
  return <Container>MainPage 입니다.</Container>;
};

const Container = styled.div``;

export default MainPage;
